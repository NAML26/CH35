/*
FUnciones. Son bloques de código con instrucciones que se pueden reutilizar las veces que sean necesarias.
Algunas funciones reciben parametros y otras no reciben parametros*/

//Funcion que permita saludar
function saludar() {
    console.log("hola desde una función");
}
// Ejecutar la funcion con el nombre de la función y los parámetros.
saludar();

//Funciones que retornan algo

function saludar () {
    console.log("Hola")
}
saludar();

//Hoisting. Caracteristica de una función que nos permite invocarla desde antes de su iniciación, es decir, se puede invocar antes del bloque de código o después. 

